import { ChartNode, NodeInputDefinition, NodeOutputDefinition } from '../NodeBase.js';
import { NodeImpl, NodeUIData } from '../NodeImpl.js';
import { Inputs, Outputs } from '../GraphProcessor.js';
import { EditorDefinition } from '../../index.js';
export type RandomNumberNode = ChartNode<'randomNumber', RandomNumberNodeData>;
export type RandomNumberNodeData = {
    min?: number;
    max?: number;
    integers?: boolean;
    maxInclusive?: boolean;
    useMinInput?: boolean;
    useMaxInput?: boolean;
};
export declare class RandomNumberNodeImpl extends NodeImpl<RandomNumberNode> {
    static create(): RandomNumberNode;
    getInputDefinitions(): NodeInputDefinition[];
    getOutputDefinitions(): NodeOutputDefinition[];
    getEditors(): EditorDefinition<RandomNumberNode>[];
    getBody(): string | undefined;
    static getUIData(): NodeUIData;
    process(inputs: Inputs): Promise<Outputs>;
}
export declare const randomNumberNode: import("../NodeImpl.js").NodeDefinition<RandomNumberNode>;
