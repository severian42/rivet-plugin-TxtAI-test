import { NodeImpl, NodeUIData } from '../NodeImpl.js';
import { ChartNode, NodeConnection, NodeInputDefinition, NodeOutputDefinition } from '../NodeBase.js';
import { Inputs, Outputs } from '../GraphProcessor.js';
import { EditorDefinition } from '../EditorDefinition.js';
export type JoinNode = ChartNode<'join', JoinNodeData>;
export type JoinNodeData = {
    flatten?: boolean;
    joinString: string;
    useJoinStringInput?: boolean;
};
export declare class JoinNodeImpl extends NodeImpl<JoinNode> {
    #private;
    static create: () => JoinNode;
    getInputDefinitions(connections: NodeConnection[]): NodeInputDefinition[];
    getOutputDefinitions(): NodeOutputDefinition[];
    getEditors(): EditorDefinition<JoinNode>[];
    getBody(): string | undefined;
    static getUIData(): NodeUIData;
    process(inputs: Inputs): Promise<Outputs>;
}
export declare const joinNode: import("../NodeImpl.js").NodeDefinition<JoinNode>;
